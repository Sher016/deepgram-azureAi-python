USER_PROMPT = (
    "Analyze this document and identify its type (e.g. cedula, pasaporte, factura, etc.). "
    "Extract all visible fields and their values. "
    "Return a JSON with two keys: 'document_type' (string) and 'fields' (object with extracted key-value pairs)."
)